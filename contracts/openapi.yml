openapi: 3.0.3
info:
  title: Car Maintenance Tracker API
  description: API for managing users, cars, and maintenance logs.
  version: 1.0.0

servers:
  - url: http://localhost:8080/api
    description: Local environment

paths:
  /health:
    get:
      summary: Health check
      responses:
        '200':
          description: OK

